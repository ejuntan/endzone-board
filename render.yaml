services:
  - type: web
    name: endzone-td-board
    runtime: python
    plan: free
    buildCommand: "bash build_site.sh"
    startCommand: "python3 -m http.server $PORT --directory public"
    autoDeploy: true
